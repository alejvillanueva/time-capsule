/* eslint-disable @typescript-eslint/no-extraneous-class */
import { Request, Response, NextFunction } from "express";

import { CapsuleService } from "../services";

export class CapsuleController {
	public static getAllCapsules = async (
		req: Request,
		res: Response,
		next: NextFunction,
	) => {
		try {
		} catch (error) {
			next(error);
		}
	};

	public static getSingleCapsule = async (
		req: Request,
		res: Response,
		next: NextFunction,
	) => {
		try {
		} catch (error) {
			next(error);
		}
	};

	public static updateCapsule = async (
		req: Request,
		res: Response,
		next: NextFunction,
	) => {
		try {
		} catch (error) {
			next(error);
		}
	};

	public static createCapsule = async (
		req: Request,
		res: Response,
		next: NextFunction,
	) => {
		try {
		} catch (error) {
			next(error);
		}
	};

	public static deleteCapsule = async (
		req: Request,
		res: Response,
		next: NextFunction,
	) => {
		try {
		} catch (error) {
			next(error);
		}
	};
}
